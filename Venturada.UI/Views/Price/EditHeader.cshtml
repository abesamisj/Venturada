@model Venturada.UI.Models.ProductHeaderModel
@{
    ViewBag.Title = "Price List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Edit Table Header</h1>
            <ol class="breadcrumb">
                <li>@Html.ActionLink("Home", "Index", "Home")</li>
                <li>Edit Header</li>
            </ol>
        </div>
    </div><!-- /.row -->
    <div class="row">
        <div class="col-sm-12">
            <div class="row">
                @using (Html.BeginForm("EditHeader", "Price"))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group col-sm-12">
                        <label for="input3">Category</label>
                        @Html.TextBoxFor(model => model.ProductTableTitleDescription, new { maxlength = 100, @class = "form-control" })
                    </div>

                    <div class="form-group col-lg-12">
                        <input type="hidden" value="@Model.ProductLabelId" name="productLabelId">

                        <button class="btn btn-primary" type="submit">Save</button>
                    </div>
                }
            </div>
        </div>
    </div>


</div>

